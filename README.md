# Myrsinie — personal portfolio

A responsive React portfolio built with Vite and deployed on GitHub Pages.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
npm run preview
```

Pushing to `main` triggers the GitHub Pages workflow in `.github/workflows/deploy.yml`.
